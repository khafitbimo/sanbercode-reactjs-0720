var daftarAlatTulis = ["2. Pensil","5. Penghapus","3. Pulpen","4. Penggaris","1. Buku"];

var daftarSort = daftarAlatTulis.sort();
var flag = 0;
while (flag < daftarSort.length) {
    console.log(daftarSort[flag]);
    flag++;
}